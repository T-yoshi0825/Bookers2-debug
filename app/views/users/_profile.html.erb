<table class="table">
		<tr><%= attachment_image_tag users, :profile_image, size: "100x100", format: 'jpeg', fallback: "no_image.jpg" %></tr>

		<tr>
			<th>name</th>
			<th><%= users.name %></th>
		</tr>

		<tr>
			<th>introduction</th>
			<th><%= users.introduction %></th>
		</tr>
</table>

<div class="row">
	<%= link_to '',edit_user_path(current_user.id), class: " btn btn-default btn-block glyphicon glyphicon-wrench edit_user_#{current_user.id}" %>
</div>